<h1>Hacker News</h1>
<% @posts.each do |post| %>
  <div>
    <h3><a href="<%= post.url %>"target="_blank"><%= post.title %></a></h3>
    <span>
      <a href="/vote/<%= post.id %>">▴</a>
      <%= post.votes %>
    </span>
  </div>
<% end %>

<a href="/new">Add post</a>
